/*
 * s01e04 — каркас модуля crypto (реализация).
 */
#include "crypto.h"

/* TODO: реализуй xor_crypt() — XOR каждого байта buf[i] ключом key, на месте. */

/* TODO: реализуй checksum() — верни аддитивную сумму байтов (unsigned char). */
