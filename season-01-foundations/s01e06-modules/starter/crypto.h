/*
 * s01e04 — каркас модуля crypto (интерфейс).
 * Скопируй папку starter/ в artifacts/ и закрой TODO.
 */
#ifndef CRYPTO_H
#define CRYPTO_H

/* TODO: объяви функции модуля.
 * void xor_crypt(unsigned char *buf, int len, unsigned char key);
 * unsigned char checksum(const unsigned char *buf, int len);
 */

#endif /* CRYPTO_H */
