# Season 1 — Foundations

**Основы C через расследование. 5 коротких серий, ~3.5 часа.**

> *«Всё начинается с простого. Но ничто не кончается так, как начиналось».*

Ты — Александр «Морфеус» Соколов, специалист по кибербезопасности. В 23:47 на старый канал приходит сообщение от Виктора Петрова — напарника, который два года как мёртв. Расшифровав его, ты втягиваешься в операцию MOONLIGHT. Твой напарник по коду — **LUNA**, ИИ Виктора; в реальности это твой ассистент в Cursor.

Season 1 закладывает фундамент C: от байтов и XOR до функций, структур и сборки. К финалу собран первый инструмент — `moonlight_decoder` — и он вскрывает файл, меняющий всё.

---

## Серии

| ID | Название | Концепт | Артефакт |
|----|----------|---------|----------|
| [s01e01](s01e01-strange-message/) | Странное сообщение | байты, типы, XOR | `decoder.c` |
| [s01e02](s01e02-chain-of-clues/) | Цепочка зацепок | условия (`if`/`switch`) | `classifier.c` |
| [s01e03](s01e03-patterns-in-time/) | Паттерны во времени | циклы (`for`/`while`) | `pattern_finder.c` |
| [s01e04](s01e04-first-tool/) | Первый инструмент | функции, модули `.h`/`.c` | модуль `crypto` |
| [s01e05](s01e05-assemble-tool/) | Сборка инструмента | `struct`/`typedef`, сборка | `moonlight_decoder` |

Каждая серия: один концепт, одна задача, ~40–55 минут. README по шаблону v2.0 (холодное открытие → теория → проверки от LUNA → задача → разбор ошибок → клиффхенгер). Теория «вглубь» и ссылки на книги — в `theory.md` каждой серии.

## Как проходить

Оболочка — **Cursor в режиме IDE**. LUNA из сюжета = чат Cursor в реальности: спрашивай «почему», а не «сделай за меня».

```bash
cd s01e01-strange-message
cat README.md
cp starter.c artifacts/decoder.c   # в s01e04–05: cp -r starter/* artifacts/
# ... пишешь код ...
make test                          # зелёный тест = серия пройдена
```

Эталон в `solution/` открывай только после честной попытки.

## Season Project: `moonlight_decoder`

Артефакты серий не выбрасываются — они собираются в сквозной инструмент сезона: модуль `crypto` (XOR, checksum), классификатор записей, искатель паттернов, отчёт на структурах. В следующих сезонах он переиспользуется через `#include`.

## Персонажи

- **LUNA** — ИИ Виктора и твой тьютор; сухой ум, дозированный юмор.
- **Виктор Петров** — напарник, «погибший» два года назад. Финал s01e05: жив.
- **Призрак** (`:)`) — аноним, знающий твой закрытый канал (s01e02). Кто он — вопрос Season 3.

## Что дальше

Финал разблокирует **Season 2 — Memory & Pointers** (`s02e01…`): указатели, `malloc`/`free`, утечки.

---

<sub>Season 1 v2.0: пересобран в атомарные серии `s01e01…s01e05`. Соответствие со старой нумерацией: Episode 01 → s01e01; 02 → s01e02; 03 → s01e03; Episode 04 (2424 строки) → разбит на s01e04 + s01e05. Прежние папки `episode-01…04/` и устаревшие season-документы (`CHANGELOG.md`, `IMPROVEMENTS_SUMMARY.md`, `season_project_*`) удалены; их история сохранена в git (коммиты до v2.0). Season Project `moonlight_decoder` теперь собирается внутри серий и финализируется в `s01e05`.</sub>
