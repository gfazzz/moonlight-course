# s10e06 — Дашборд операции

**Season 10: Final Mission · Серия 6 из 8 · ~70 мин**
Концепт: отделение модели от представления; экран показывает не только значения, но их возраст и доступность. Задача: собрать дашборд операции.

> *«Устаревшие данные, выглядящие свежими, опаснее их отсутствия».*

---

## Previously

Приёмный тракт держит враждебный эфир: пакет с заявленной длиной больше фактической отвергается, арифметика не переполняется, санитайзеры молчат.

Данные идут, проверяются и накапливаются. Оператор их не видит.

---

## Сцена

Стэнфорд, 02:15. Четыре источника телеметрии, семь подсистем, окно связи, которое открывается и закрывается. Всё это лежит в структурах и печатается в лог построчно — читать невозможно.

```
[LUNA] Нужен один экран, на котором видно операцию целиком.
[LUNA] Возьми рендер в буфер из s09e06 — терминал для этого не нужен.
[LUNA] И два правила, которые здесь важнее рисования.
[LUNA] Первое: модель отдельно, представление отдельно. Экран ничего не вычисляет.
[LUNA] Второе — и оно спасает операции.
```

Пауза.

```
[LUNA] Показывай не только значение, но и его ВОЗРАСТ.
[LUNA] Датчик, который замолчал сорок секунд назад, продолжает показывать
[LUNA] последнее известное число. Оно выглядит нормальным. Оно ложь.
[LUNA] Оператор примет решение по мёртвым данным и не поймёт, что случилось.
```

Это не абстракция. Ровно так теряют аппараты: значение на экране есть, а откуда оно и когда получено — нет.

---

## Что ты освоишь

- Разделение модели и представления: экран как чистая функция от состояния.
- Индикацию возраста и доступности данных наравне со значениями.
- Рендер в буфер фиксированного размера — детерминированный и тестируемый.

Глубже — в [`theory.md`](theory.md).

---

## Теория: экран не должен врать

### Модель отдельно от представления

```c
typedef struct {            /* МОДЕЛЬ: что известно об операции */
    Source  src[4];
    double  window_min, elevation_deg;
    int     up, total;
} OpState;

void render(const OpState *s, char scr[H][W]);   /* ПРЕДСТАВЛЕНИЕ */
```

`render` помечен `const` не случайно: он **не имеет права** ничего вычислять или менять. Всё, что показано на экране, вычислено до него. Иначе экран становится вторым источником истины, и два источника рано или поздно разойдутся.

### Три состояния источника, а не два

| Состояние | Что значит | Как показать |
|---|---|---|
| `ok` | данные свежие | значение и возраст |
| `stale` | данные старше порога | значение, но пометкой `[OLD]` |
| `down` | данных нет вовсе | `---`, `no data` |

Ошибка проектирования — сводить это к двум состояниям «есть значение / нет значения». Устаревшее значение есть, оно правдоподобно, и именно поэтому опасно.

Порог устаревания — часть контракта, а не украшение: `STALE_SEC 30` означает, что данные старше тридцати секунд считаются недостоверными.

### Рендер в буфер

Экран собирается в массив `char scr[H][W]` и печатается одним куском. Три следствия, все полезные:

- **тестируемость** — буфер можно сравнить с эталоном посимвольно;
- **детерминизм** — никаких управляющих последовательностей и зависимости от терминала;
- **атомарность** — пользователь не видит наполовину отрисованный кадр.

Подписи внутри экрана — **только ASCII**: ячейка буфера равна одному байту, и кириллица разъедет разметку. Это то же ограничение, что в s09e06, и здесь оно жёстче, потому что кадр сравнивается с эталоном.

---

## Проверка на канале

```
[LUNA] Два вопроса. От второго зависит, доверять ли экрану вообще.
```

**1.** LUNA: «Почему `render` принимает состояние по `const`-указателю и ничего не считает сам?»

<details><summary>Ответ</summary>

Потому что иначе появляется второй источник истины. Если экран досчитывает что-то на лету, его значения могут разойтись с моделью — и разойдутся, как только логику поправят в одном месте и забудут в другом. `const` фиксирует контракт: представление отображает, а не вычисляет. Побочный эффект — `render` становится чистой функцией, и его можно проверять сравнением с эталонным кадром.
</details>

**2.** LUNA: «Датчик молчит сорок секунд. Почему показать последнее значение без пометки хуже, чем не показать ничего?»

<details><summary>Ответ</summary>

Потому что отсутствие данных оператор заметит и начнёт разбираться, а правдоподобное старое значение он примет за текущее. Экран не соврал формально — он показал то, что получил, — но решение будет принято по несуществующей реальности. Поэтому возраст данных отображается наравне со значением, а превышение порога помечается явно.
</details>

```
[LUNA] Верно. Собирай экран.
```

---

## Задача: `dashboard.c`

Собрать дашборд операции: источники телеметрии с возрастом и состоянием, окно связи, статус подсистем, сводный статус. Три сценария — штатный, устаревание данных, потеря источника и деградация ядра.

Режим — **максимальная автономия**: спецификация задаётся видом экрана в [`tests/expected.txt`](tests/expected.txt), требования — в [`mission.md`](mission.md), каркас — в [`starter.c`](starter.c).

```bash
cp starter.c artifacts/dashboard.c
make test
```

Ожидаемо — `PASS`: три кадра совпадают с эталоном; сводный статус `NOMINAL` → `STALE DATA` → `DEGRADED`. Эталон — [`solution/dashboard.c`](solution/dashboard.c).

---

## Разбор типичных ошибок

1. **Вычисления внутри `render`.** Модель и представление разъедутся.
2. **Два состояния вместо трёх.** Устаревшее значение неотличимо от свежего.
3. **Кириллица внутри экрана.** Ячейка — байт; разметка поедет, тест упадёт.
4. **Печать по мере отрисовки.** Кадр рвётся; сравнить с эталоном невозможно.
5. **Полоса, считающая проценты от неверного знаменателя.** `up/total`, а не `up/4`.
6. **Возраст, посчитанный от системного времени.** Тест перестанет быть детерминированным — возраст приходит в модели.

---

## Debriefing

Три кадра. В первом всё зелёное. Во втором два источника помечены `[OLD]`, и сводный статус сменился на `STALE DATA` — значения на месте, но им нельзя верить. В третьем `uplink` отвалился совсем, окно связи закрылось, подсистем осталось пять из семи: `DEGRADED`.

Ни один из трёх экранов не притворяется, что всё хорошо.

```
[LUNA] Хорошо. Теперь оператор видит не картинку, а состояние.
[LUNA] Осталось соединить всё в один поток: приём, проверку, обработку, показ.
[LUNA] По отдельности работает каждый кусок. Это ничего не значит.
[LUNA] Дальше — s10e07. Сквозной конвейер.
```

**Навык разблокирован:** разделение модели и представления, индикация возраста и доступности, детерминированный рендер в буфер.

---

## Ссылки

- **Tufte, «The Visual Display of Quantitative Information»** — почему отсутствие данных надо показывать так же явно, как данные.
- **Nygard, «Release It!», гл. про мониторинг** — устаревшая метрика опаснее отсутствующей.

Разбор и промпты для LUNA — в [`theory.md`](theory.md).

---

## Навигация

- Назад: [s10e05 — Защита приёмного тракта](../s10e05-hardening/)
- Season 10 — обзор: [../README.md](../README.md)
- Дальше: **[s10e07 — Сквозной конвейер](../s10e07-pipeline/)**
