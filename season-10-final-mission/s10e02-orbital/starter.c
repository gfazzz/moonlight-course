/*
 * OPERATION MOONLIGHT — s10e02 "Орбитальная механика"
 * Каркас. Скопируй в artifacts/orbit.c и реализуй.
 *
 *   cp starter.c artifacts/orbit.c
 *   make test
 *
 * МАКСИМАЛЬНАЯ АВТОНОМИЯ: сигнатур нет, структуру выбираешь сам.
 * Физика и критерии — в mission.md, формат вывода — в tests/expected.txt.
 *
 * Что понадобится (и чего нет в стандартной библиотеке без -lm):
 *   my_sqrt  — метод Ньютона;
 *   my_sin / my_cos — ряды Тейлора с приведением аргумента к [-PI, PI];
 *   my_atan2 — ряд для atan + разбор квадрантов.
 *
 * Ядро задачи — уравнение Кеплера:  M = E - e*sin(E)
 * Оно трансцендентное: E через M не выражается. Решай Ньютоном:
 *   f(E)  = E - e*sin(E) - M
 *   f'(E) = 1 - e*cos(E)
 *   E    -= f(E)/f'(E)      до сходимости (|dE| < 1e-13)
 */
#include <stdio.h>

#define PI 3.14159265358979323846
#define MU 398600.4418          /* гравитационный параметр Земли, км^3/с^2 */
#define RE 6378.137             /* радиус Земли, км */

int main(void) {
    const double a = 7000.0;    /* большая полуось, км */
    const double e = 0.02;      /* эксцентриситет */

    printf("=== орбитальная механика: где спутник ===\n");
    (void)a; (void)e;
    return 0;
}
